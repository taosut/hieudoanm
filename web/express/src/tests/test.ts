'use strict';

import assert from 'assert';
import { describe, it } from 'mocha';

describe('test', () => {
  it('dumb test', async () => {
    console.log('dumb test');
    assert.ok(true);
  });
});
